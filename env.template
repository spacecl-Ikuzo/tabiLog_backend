# Tabilog Backend 환경 변수 템플릿
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값들을 입력하세요
# .env 파일은 절대 Git에 커밋하지 마세요!

# ===========================================
# 데이터베이스 설정
# ===========================================
# 개발 환경용 데이터베이스 URL
DB_URL=jdbc:mysql://34.64.56.243:3306/tabilog?useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Seoul

# 데이터베이스 사용자명
DB_USERNAME=tabilog

# 데이터베이스 비밀번호 (실제 비밀번호로 변경 필요)
DB_PASSWORD=your_database_password_here

# ===========================================
# JWT 보안 설정
# ===========================================
# JWT Secret 키 (Base64 인코딩된 문자열)
# 새로 생성하려면: echo -n "your_secret_string_here" | base64
JWT_SECRET=your_jwt_secret_base64_encoded_here

# ===========================================
# Google Maps API
# ===========================================
# Google Maps API 키
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# ===========================================
# Gmail SMTP 설정
# ===========================================
# Gmail 계정 (앱 비밀번호 사용 권장)
GMAIL_USERNAME=your_email@gmail.com

# Gmail 앱 비밀번호 (일반 비밀번호 아님!)
GMAIL_APP_PASSWORD=your_gmail_app_password_here

# ===========================================
# 프론트엔드 URL 설정
# ===========================================
# 프론트엔드 애플리케이션 URL
FRONTEND_URL=http://localhost:5173

# ===========================================
# 보안 주의사항
# ===========================================
# 1. 이 파일을 복사하여 .env 파일을 생성하세요
# 2. .env 파일을 .gitignore에 추가하세요
# 3. 실제 운영 환경에서는 더 강력한 비밀번호를 사용하세요
# 4. JWT Secret은 충분히 길고 복잡한 문자열을 사용하세요
# 5. Gmail 앱 비밀번호는 Google 계정 설정에서 생성하세요
# 6. Google Maps API 키에는 적절한 제한을 설정하세요
