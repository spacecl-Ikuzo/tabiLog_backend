# 개발 환경 설정 (로컬 MySQL)
spring.application.name=Tabilog

# 서버 포트
server.port=8080

# 로컬 MySQL 데이터베이스 설정
spring.datasource.url=jdbc:mysql://localhost:3306/tabilog_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Shka991204!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 로깅 설정
logging.level.org.springframework.web=DEBUG
logging.level.com.ikuzo.tabilog=DEBUG

# CORS 설정 (개발 환경)
cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:5173,http://localhost:8080,http://localhost:4200,http://127.0.0.1:3000,http://127.0.0.1:3001,http://127.0.0.1:5173